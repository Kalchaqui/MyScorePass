{
  "name": "myscorepass-contracts",
  "version": "1.0.0",
  "description": "Smart Contracts para MyScorePass (SBT + Identity)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:fuji": "hardhat run scripts/deploy.js --network avalancheFuji",
    "deploy:testnet": "hardhat run scripts/deploy.js --network moonbase",
    "deploy:paseo": "hardhat run scripts/deploy.js --network paseo"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0"
  }
}


